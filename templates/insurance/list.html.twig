{% extends 'navbar3.html.twig' %}
{% block body %}
 <div class="container">
 <!--h3>Utilisateur connecte :{#{ ema }#}</h3>  
 <div class="alert alert-primary" role="alert">
   <strong>{#{ msg }#}</strong>
 </div-->
  <table class="table table-striped ">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">{{'NAME' | trans }}</th>
      <th scope="col">{{'DISCOUNT' | trans }}</th>
      <th scope="col">ACTIONS</th>
    </tr>
  </thead>
  <tbody>
   {% for i in insurances %}
    <tr>
      <th scope="row">{{ i.id }}</th>
      <td>{{ i.name }}</td>
      <td>{{ i.discount }}</td>
      <td><a class="btn btn-danger" href="{{ path("app_insurance_delete" ,{'id':i.id} ) }}">  {{'DELETE' | trans }}</a>
      &nbsp &nbsp &nbsp &nbsp
      <a class="btn btn-primary" href="{{ path("app_insurance_edit" ,{'id':i.id} ) }}">   {{'CHANGE' | trans }} </a>
    </tr>
     {% endfor %}
      </tbody>
</table>
</div>
{% endblock %}