{% extends 'navbar3.html.twig' %}
{% block body %}
 <div class="container">
 <!--h3>Utilisateur connecte :{#{ ema }#}</h3>  
 <div class="alert alert-primary" role="alert">
   <strong>{#{ msg }#}</strong>
 </div-->
  <table class="table table-striped ">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">{{'NAME' | trans }}</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
   {% for s in specialities %}
    <tr>
      <th scope="row">{{ s.id }}</th>
      <td>{{ s.name }}</td>
      <td><a class="btn btn-danger" href="{{ path("app_speciality_delete" ,{'id':s.id} ) }}">{{'Delete' | trans }} </a>
           &nbsp &nbsp &nbsp &nbsp
      <a class="btn btn-primary" href="{{ path("app_speciality_edit" ,{'id':s.id} ) }}">  {{'Change' | trans }}</a>
    </tr>
     {% endfor %}
      </tbody>
</table>
</div>
{% endblock %}